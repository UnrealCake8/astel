import {NextRequest} from "next/server";import {proxy} from "../_daemon";export const runtime="nodejs";export async function POST(req:NextRequest){const {id,text}=await req.json();if(!id||!text)return Response.json({error:"id and text required"},{status:400});const r=await proxy("/calls/"+encodeURIComponent(id)+"/speak",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({text})});return new Response(await r.text(),{status:r.status,headers:{"Content-Type":"application/json"}})}
