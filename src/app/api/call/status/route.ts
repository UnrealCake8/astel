import {NextRequest} from "next/server";import {proxy} from "../_daemon";export const runtime="nodejs";export async function GET(req:NextRequest){const id=req.nextUrl.searchParams.get("id");if(!id)return Response.json({error:"id required"},{status:400});const r=await proxy("/calls/"+encodeURIComponent(id));return new Response(await r.text(),{status:r.status,headers:{"Content-Type":"application/json"}})}
