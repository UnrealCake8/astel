import {NextRequest} from "next/server";import {proxy} from "../_daemon";export const runtime="nodejs";export async function POST(req:NextRequest){const {id,digits}=await req.json();const r=await proxy("/calls/"+encodeURIComponent(id)+"/dtmf",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({digits})});return new Response(await r.text(),{status:r.status,headers:{"Content-Type":"application/json"}})}
